package hub

func Begin() {

	// // create an accessory
	// info := accessory.Info{Name: "Lamp"}
	// ac := accessory.NewSwitch(info)

	// // configure the ip transport
	// config := hc.Config{Pin: "00102003", StoragePath: ".erika"}
	// t, err := hc.NewIPTransport(config, ac.Accessory)
	// if err != nil {
	// 	log.Panic(err)
	// }

	// ac.Switch.On.OnValueRemoteUpdate(func(b bool) {
	// 	fmt.Println(b)
	// })

	// hc.OnTermination(func() {
	// 	<-t.Stop()
	// })

	// t.Start()
}
